{% block body %}
<div class="container">
	<div class="popup">
<div class="row news_detail">
    <div class="col-md-12 companylist_container newsdetilspage">
	  {% if asData|length > 0%}
		{% for data in asData%}  
		<h3>{{ data['title'] }}</h3>		
		<p>{{ data['description']|raw }}</p>
	  {% endfor %}
	  {% endif %}
	</div>	
</div>
</div>
</div>
{% endblock %}